[tool.uv]
dev-dependencies = [
    "ipykernel>=6.29.5",
    "jupyter>=1.0.0",
    "pyright>=1.1.377",
    "ruff>=0.6.2",
]

[tool.uv.workspace]
members = [
    "apps/py/*",
    "packages/py/*",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
